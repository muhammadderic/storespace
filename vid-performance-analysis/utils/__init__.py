# Import specific functions or modules for easier access
from .helper_functions import load_data

# Initialize any package-wide constants or settings
PACKAGE_NAME = "Utils Package"
VERSION = "1.0.0"

# You can print a message when the package is imported (optional)
print(f"{PACKAGE_NAME} (version {VERSION}) has been loaded.")